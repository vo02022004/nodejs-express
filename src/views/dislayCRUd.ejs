<!DOCTYPE html>
<html>

<head>
    <style>
        tr,
        table,
        th {
            padding: 0;
            margin: auto;
        }

        .btn-edit {
            text-decoration: none;
            background: yellow;
            cursor: pointer;
            padding: 1px 16px;
        }

        .btn-delete {
            background: rgb(255, 55, 0);
            text-decoration: none;
            padding: 1px 10px;
            cursor: pointer;
        }

        .btn-delete:hover,
        .btn-edit:hover {
            animation: noi 0.2s linear infinite;
        }

        @keyframes noi {
            0% {
                /* transform: scale(0.9); */
                transform: rotate(0deg);
                opacity: 1;
            }

            100% {
                /* transform: scale(1); */
                transform: rotate(360deg);

                background: orange;
            }
        }
    </style>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>

<body>

    <h2 class="text-center">LIST USER</h2>
    <table class="bg-secondary table table-bordered " style="width: 1300px; height: auto; background: gray;">
        <tr>
            <th>Email</th>
            <th>Firstname</th>
            <th>Lastname</th>
            <th>Address</th>
            <th>Acction</th>
        </tr>
        <% data.map((row, index)=> { %>
            <tr>
                <td>
                    <%= row.email %>
                </td>
                <td>
                    <%= row.firstName %>
                </td>
                <td>
                    <%= row.lastName %>
                </td>
                <td>
                    <%= row.address %>
                </td>
                <td style="width: 160px;">
                    <button type="button" class="btn-edit"
                        onclick="window.location.href='/edit-user?id=<%=row.id%>'">Edit</button>
                    <button type="button" class="btn-delete"
                        onclick="window.location.href='/delete-crud?id=<%=row.id%>'">Delete</button>
                </td>
            </tr>
            <% }) %>
    </table>
    <button style="margin-left:120px; background: green;" onclick="window.location.href ='/create-user'">Add
        User</button>

</body>

</html>